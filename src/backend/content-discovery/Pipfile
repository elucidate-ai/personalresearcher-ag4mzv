[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[requires]
python_version = "3.11"

[packages]
# Web Framework and API - v0.104.0 for latest stable features and security updates
fastapi = "^0.104.0"
uvicorn = "^0.24.0"
pydantic = "^2.0.0"
pydantic-settings = "^2.0.0"

# Task Queue and Caching - v5.3.0 for distributed processing
celery = "^5.3.0"
redis = "^5.0.0"

# Database Drivers - v4.6.0 for latest MongoDB compatibility
pymongo = "^4.6.0"

# Content Source APIs - Latest stable versions for API compatibility
google-api-python-client = "^2.0.0"
spotipy = "^2.23.0"

# Data Processing and Analysis - Latest versions for performance
numpy = "^1.26.0"
pandas = "^2.1.0"
scikit-learn = "^1.3.0"

[dev-packages]
# Testing - Latest versions for comprehensive test coverage
pytest = "^7.4.0"
pytest-cov = "^4.1.0"
pytest-asyncio = "^0.21.0"

# Code Quality - Latest versions for modern Python practices
black = "^23.10.0"
isort = "^5.12.0"
flake8 = "^6.1.0"
mypy = "^1.6.0"