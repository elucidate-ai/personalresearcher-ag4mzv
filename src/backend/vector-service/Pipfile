[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# Web Framework and Server
fastapi = "^0.104.0"  # High-performance async web framework
uvicorn = "^0.24.0"  # ASGI server implementation

# gRPC Communication
grpcio = "^1.59.0"  # gRPC runtime library
grpcio-tools = "^1.59.0"  # gRPC tools for code generation

# Configuration and Validation
python-dotenv = "^1.0.0"  # Environment variable management
pydantic = "^2.4.2"  # Data validation using Python type annotations

# Machine Learning and Vector Processing
torch = "^2.1.0"  # PyTorch for GPU-accelerated vector operations
numpy = "^1.26.0"  # Numerical computing library
transformers = "^4.35.0"  # Hugging Face Transformers for embeddings

# Vector Database and Caching
pinecone-client = "^2.2.4"  # Pinecone vector database client
redis = "^5.0.1"  # Redis client for caching

# Utilities
tenacity = "^8.2.3"  # Retry library for robust operations

[dev-packages]
# Testing
pytest = "^7.4.3"  # Testing framework
pytest-cov = "^4.1.0"  # Test coverage reporting

# Code Quality
black = "^23.10.0"  # Code formatter
isort = "^5.12.0"  # Import sorter
flake8 = "^6.1.0"  # Code linter
mypy = "^1.6.1"  # Static type checker

[requires]
python_version = "3.11"